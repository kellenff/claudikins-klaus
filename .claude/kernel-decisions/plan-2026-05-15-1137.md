# Kernel Decision Log — plan-2026-05-15-1137

Task: Design an adaptation of Klaus for poking holes in logic — prose and structured forms — leveraging the @casualtheorics/argdown skill.

---

## outline.phase-0.session-init

- **severity:** trivial
- **decision:** New Plan — fresh session initialised
- **rationale:** "(no annotation; no prior session existed)"
- **considered_options:**
  - Resume — why_rejected: no prior session to resume
  - New Plan — chosen
  - Review Last — why_rejected: no prior session to review

---

## outline.phase-1.continue

- **severity:** notable
- **decision:** Continue to Research
- **rationale:** "(no annotation; selected from option list)". Requirements as understood: build a slash command + agent + skill in the claudikins-klaus plugin for adversarial argument auditing; sibling persona to Klaus with Germanic-rigour DNA; accepts prose or Argdown via three invocation modes (empty=interactive, file=audit, inline=audit); adaptively routes through argdown plugin skills (extract → audit → rebut → Dung extensions as needed); flag-driven output (inline default, --report writes audit report, --argdown writes annotated artifact); v1 includes multi-document corpus analysis with cross-document synthesis (flagged as ambitious); out of scope: editing source prose, non-argdown logic systems, cross-session persistence.
- **considered_options:**
  - Continue to Research — chosen
  - Revise Requirements — why_rejected: user accepted requirements as summarised
  - Abandon Plan — why_rejected: user proceeded

---

## outline.phase-2.mode-selection

- **severity:** trivial
- **decision:** Selected research modes: Codebase + Docs + External (all three)
- **rationale:** "(no annotation; selected from option list)"
- **considered_options:**
  - Codebase + Docs — why_rejected: user opted for fuller coverage including external prior art
  - All three (Codebase + Docs + External) — chosen
  - Codebase only — why_rejected: docs/external value judged worth the time
  - Skip research entirely — why_rejected: confidence cost too high

---

## outline.phase-2.continue

- **severity:** trivial
- **decision:** Continue to Approaches
- **rationale:** "(no annotation; selected from option list)". Most salient finding: argdown ships seven skills (not six) plus three MCP tools; Toulmin/Walton roles are NOT first-class in the DSL — must be encoded as YAML metadata; existing `commands/klaus.md` has a name-drift bug (declares `rigorous-debugging`, real skill is `debugging`) that the new triple must avoid.
- **considered_options:**
  - Continue to Approaches — chosen
  - Back to Brain-jam — why_rejected: research findings reinforce rather than overturn requirements
  - Skip / Abandon — why_rejected: user proceeded

---

## outline.phase-3.approach-selection

- **severity:** architectural
- **decision:** Adopt Approach B — agent + thick methodology skill mirroring klaus + debugging shape; 7-phase argument-audit skill with sparring as terminal phase
- **rationale:** "(no annotation; selected from option list)". Recommendation reasoning: B matches the plugin's established klaus.md + debugging shape and is the path of least surprise; A under-delivers on success criteria 1 (non-obvious weaknesses) and 4 (clean round-trip) because both require methodology beyond delegation; C's split between audit and sparring skills creates discovery friction without distinct workflows since sparring is naturally the terminal phase of an audit, not a parallel concern.
- **considered_options:**
  - Approach A — Thin orchestrator: skill is a routing tree over existing argdown skills, low effort, low risk. why_rejected: under-delivers on success criteria 1 and 4; methodology depth too thin for "non-obvious weaknesses" goal
  - Approach B — Thick auditor with own methodology skill: 7-phase audit, sparring as terminal phase, mirrors klaus+debugging. why_rejected: n/a (chosen)
  - Approach C — Layered trio: separate audit and sparring skills. why_rejected: discovery friction, sparring/audit split not distinct enough to justify two skills
  - Revise Approaches — why_rejected: B accepted as drafted

---

## outline.phase-4.section.problem

- **severity:** trivial
- **decision:** Approved Problem Statement; persona committed to chess-grandmaster (Aron, after Nimzowitsch)
- **rationale:** "(no annotation; selected from option list)". Persona maps onto argument analysis well (opening/middlegame/endgame, prophylaxis, zugzwang); sibling to Klaus without overlap.
- **considered_options:**
  - Continue — chosen
  - Revise section — why_rejected: user accepted draft
  - Back to Approaches — why_rejected: user proceeded
  - Abandon — why_rejected: user proceeded

---

## outline.phase-4.section.scope

- **severity:** notable
- **decision:** Approved Scope & Boundaries; corpus mode flagged as ambitious for reviewer
- **rationale:** "(no annotation; selected from option list)". Scope captures three invocation modes, bidirectional prose↔Argdown, four Walton schemes, Rapoport steelman, Pollock distinction, Dung extensions, multi-doc corpus with synthesis, sparring loop with LD primitives, flag-driven output. Out of scope: editing user prose, non-argdown logic, persistence, AIF export, edits to existing Klaus trio.
- **considered_options:**
  - Continue — chosen
  - Revise section — why_rejected: user accepted draft
  - Back to Approaches — why_rejected: user proceeded
  - Abandon — why_rejected: user proceeded

---

## outline.phase-4.section.success

- **severity:** notable
- **decision:** Approved Success Criteria (10 criteria, SC-1 through SC-10)
- **rationale:** "(no annotation; selected from option list)". Each criterion independently verifiable; covers structural (SC-1, SC-2), argdown AST validation (SC-3), behavioural (SC-4 to SC-9), flag matrix (SC-10).
- **considered_options:**
  - Continue — chosen
  - Revise section — why_rejected: user accepted draft
  - Back to Approaches — why_rejected: user proceeded
  - Abandon — why_rejected: user proceeded

---

## outline.phase-4.section.tasks

- **severity:** notable
- **decision:** Approved Tasks section; 8 tasks across 4 batches with EXECUTION_TASKS markers; blast-radius low (16 new files, 1 additive README edit, 3 architecture areas)
- **rationale:** "(no annotation; selected from option list)". Decomposition: T1 (AST fixture round-trip), T2 (methodology refs), T3 (test fixtures) parallel in batch 1; T4 (skill) + T5 (agent) parallel in batch 2; T6 (command) in batch 3; T7 (README) + T8 (smoke verify) parallel in batch 4. Blast radius confirmed greenfield: 14 existing README sections at risk, all additive.
- **considered_options:**
  - Continue — chosen
  - Revise section — why_rejected: user accepted draft
  - Back to Approaches — why_rejected: user proceeded
  - Abandon — why_rejected: user proceeded

---

## outline.phase-4.section.deps

- **severity:** notable
- **decision:** Approved Dependencies section; argdown-plugin is hard runtime dependency with graceful-degradation path on absence
- **rationale:** "(no annotation; selected from option list)". Hard deps: argdown-plugin (MCP tools + 7 skills), tool-executor (already used by Klaus). Soft deps: auto-discovery, MCP server, Klaus trio independence. Internal dep DAG matches task table. No new packages, no CI changes.
- **considered_options:**
  - Continue — chosen
  - Revise section — why_rejected: user accepted draft
  - Back to Approaches — why_rejected: user proceeded
  - Abandon — why_rejected: user proceeded

---

## outline.phase-4.section.risks

- **severity:** notable
- **decision:** Approved Risks & Mitigations (9 risks: R-1 through R-9)
- **rationale:** "(no annotation; selected from option list)". Headline risks: R-1 (AST shape drift, mitigated by T1 fixture gate), R-2 (corpus synthesis hardness, mitigated by literal-claim-text v1 scope), R-3 (persona buries verdict, mitigated by Verdict callout enforcement + --sober flag), R-4 (naming drift, mitigated by grep check), R-6 (argdown-plugin absent, mitigated by Phase 2 probe).
- **considered_options:**
  - Continue — chosen
  - Revise section — why_rejected: user accepted draft
  - Back to Approaches — why_rejected: user proceeded
  - Abandon — why_rejected: user proceeded

---

## outline.phase-4.section.verification

- **severity:** notable
- **decision:** Approved Verification Checklist; covers structural (SC-1, SC-2, R-4), behavioural (SC-3 to SC-10), risk-mitigation (R-3, R-6, R-7, R-9), independence
- **rationale:** "(no annotation; selected from option list)". Every SC has at least one verification item; independence verification ensures Klaus trio is untouched and removing Aron leaves Klaus working.
- **considered_options:**
  - Continue — chosen
  - Revise section — why_rejected: user accepted draft
  - Back to Approaches — why_rejected: user proceeded
  - Abandon — why_rejected: user proceeded

---

## outline.phase-5.reviewer-selection

- **severity:** trivial
- **decision:** Klaus (devil's advocate)
- **rationale:** "(no annotation; selected from option list)"
- **considered_options:**
  - Klaus (devil's advocate) — chosen
  - Skip review — why_rejected: user wanted brutal pass before finalising
  - Both perspectives — why_rejected: Klaus alone judged sufficient

---

## outline.phase-5.iterate-or-finalise

- **severity:** architectural
- **decision:** Finalise plan after absorbing Klaus's hits #1, #4, #6, #7; conscious non-application of hits #2 and #5
- **rationale:** "(no annotation; selected from option list)". Klaus reviewer verdict (verbatim summary): scored 3-0 with seven distinct hits; identified killer (one-shot agents don't loop, breaking sparring assumption), big (SC-9 contradiction-vs-equivalence scoping bug), and four notable/smaller methodology refinements. Revisions absorbed: (#7) sparring split into digest-default + --interactive opt-in via new commands/aron-spar.md follow-up command (adds T9, brings total to 9 tasks); (#1) SC-9 reworded to "shared claim asserted in one doc and attacked in another (literal-text match)"; (#4) skill split from 7 phases to 8 (classify-schemes and fire-critical-questions separated); (#6) SC-7 reworded to "traceable source-span in report; prose may paraphrase". Conscious non-applications: (#2) Walton scope kept at four named schemes; (#5) fixture realism kept as 3 synthetic. Plan locked: 9 tasks across 4 batches; 11 success criteria (SC-1 through SC-10 plus SC-8b); 9 risks (R-1 to R-9 with R-2 impact downgraded Medium→Low after SC-9 rescoping).
- **considered_options:**
  - Iterate on feedback — why_rejected: hits absorbed, no further revision needed before finalising
  - Finalise plan — chosen
  - Back to Draft — why_rejected: no section requires full rework; revisions are surgical
  - Abandon — why_rejected: user proceeded

---

## outline.handoff

- **severity:** trivial
- **decision:** Load /claudikins-kernel:execute
- **rationale:** "(no annotation; selected from option list)"
- **considered_options:**
  - Load /claudikins-kernel:execute — chosen
  - Stay here — why_rejected: user proceeded to execute
  - Done for now — why_rejected: user proceeded to execute

---

## execute.phase-0.batch-size-override

<!-- decision: none, checkpoint: execute.phase-0.batch-size-override, reason: thresholds not exceeded; max batch size is 3 (under 7), total tasks 9 (under 15) -->

---

## execute.phase-0.loc-split

<!-- decision: none, checkpoint: execute.phase-0.loc-split, reason: no task exceeded 400 LOC threshold; largest is T4 estimated ~250-400 LOC, comparable to debugging.md baseline of 145 LOC for an 8-phase skill -->

---

## execute.phase-0.context-budget.task-3

- **severity:** notable
- **decision:** Accept T3 with caveat — proceed despite hitting 10-file hard context-budget ceiling
- **rationale:** "(no annotation; selected from option list)". Files: 10 (hidden-assumption.md/.expected.md, equivocation.md/.expected.md, unsupported-leap.md/.expected.md, corpus-contradiction/{a.md, b.md}, corpus-contradiction.expected.md, sparring-trace.md); estimated LOC ~500-800 across 10 files (~50-100 per file); fixtures benefit from cohesive authoring for stylistic consistency and calibrated difficulty across cases. Splitting into T3a/T3b would fragment style without parallelism gain since both halves block T8.
- **considered_options:**
  - Accept with caveat — chosen
  - Split into T3a + T3b — why_rejected: fragments authoring style across fixtures; both halves still block T8 so no parallelism gain
  - Proceed as-is, no caveat — why_rejected: ceiling hit deserves an explicit acknowledgement in the decision log

---

## execute.phase-1.batch-start.batch-1

- **severity:** trivial
- **decision:** Execute batch 1 (3 parallel tasks: T1, T2, T3)
- **rationale:** "(no annotation; selected from option list)". Tasks T1 (argdown-ast-fixture), T2 (methodology-refs), T3 (audit-fixtures) all parallel; greenfield blast radius.
- **considered_options:**
  - Execute batch — chosen
  - Skip task — why_rejected: all three tasks required for downstream consumers
  - Reorder — why_rejected: tasks are independent; ordering immaterial
  - Pause — why_rejected: user proceeded

---

## execute.phase-3.review-failure.task-3

- **severity:** notable
- **decision:** Retry with focused babyclaude (retry_count 0 → 1); retry succeeded
- **rationale:** Reviewer verdict (verbatim from initial spec/3.json): "missing: Byte-identical verbatim sentence ... in both corpus-contradiction/a.md and corpus-contradiction/b.md as a single contiguous line, with no surrounding double-quotes that would break byte-exact match". Retry babyclaude (commit fde13ce) unwrapped the sentence in both files; re-review independently confirmed grep -F finds byte-exact match in both files. Surrounding prose remains coherent.
- **considered_options:**
  - Retry — chosen
  - Klaus intervention — why_rejected: bug was tightly scoped (single-paragraph rewrap in two files); no architectural escalation warranted
  - Human override — why_rejected: literal-text mechanism is load-bearing for SC-9; can't override
  - Skip task — why_rejected: T3 is foundational for T8 smoke verify

---

## execute.phase-4.batch-review.batch-1

- **severity:** notable
- **decision:** Fix all six important-tier code-review issues across T1, T2, T3 before merging Batch 1; spawn three focused fix-up babyclaude agents in existing worktrees
- **rationale:** T1 spec PASS, code CONCERNS (2 important — YAML-drop placement at line 761 of 842-line file conf 78, capture provenance points to local-only path conf 72); T2 spec PASS, code CONCERNS (2 important — Argdown undercutter directionality reversed conf 78, rebutting-defeater snippet malformed conf 72; both bugs duplicated in audit-report-template.md and would propagate into real Aron audit output); T3 spec PASS after retry fde13ce, code CONCERNS (1 important — sidecar narrative drift at corpus-contradiction.expected.md:35 conf 95). All structural_scope OK. T2 Argdown bugs are the load-bearing concern: T4 will copy snippets verbatim into Aron runtime output and Dung extensions will compute attacks backward. Surgical fixes, no critical issues, no architectural rework.
- **considered_options:**
  - Fix all six — chosen
  - Fix T2 Argdown bugs only — why_rejected: T1 prominence and T3 drift are cheap fixes preventing downstream confusion in T4 and T8
  - Accept all with caveats — why_rejected: T2 Argdown directionality bug would silently break Dung-extension semantics
  - Klaus review — why_rejected: issues are clear, fixes are obvious; no architectural ambiguity needs Klaus

---

## execute.phase-5.merge-decision.batch-1

- **severity:** architectural
- **decision:** Merge each branch separately with --no-ff (preserve history) — three merge commits, one per task
- **rationale:** "(no annotation; selected from option list)". Conflict check: None detected (T1 owns argdown-ast-fixture.md, T2 owns three reference docs, T3 owns 10 fixture files; non-overlapping). Three merges executed: worktree-agent-a115f2293a707692c (T1, +970 lines argdown-ast-fixture.md), worktree-agent-acb25fb998378b8f7 (T2, +957 lines across walton-schemes/methodology-references/audit-report-template), worktree-agent-ae95ba9c5988d377e (T3, +547 lines across 10 fixture files). Per-branch history preserved including T3 retry fde13ce and the three fix-up commits, supporting future review-by-blame.
- **considered_options:**
  - Merge each branch separately (preserve history) — chosen
  - Squash-merge each task — why_rejected: loses T3 retry trail and fix-up iteration history that would be useful for future blame
  - Single octopus merge — why_rejected: unconventional; harder to revert one task without others
  - Keep separate, don't merge yet — why_rejected: deferring increases risk of conflicts when Batch 2 starts touching the skill dir

---

## execute.phase-1.batch-start.batch-2

- **severity:** trivial
- **decision:** Execute batch 2 (parallel: T4 skill body + T5 Aron agent)
- **rationale:** "(no annotation; selected from option list)". Both tasks depend only on now-merged Batch 1 (T1, T2). Skill (T4) is persona-neutral; agent (T5) binds to it via skills frontmatter. Parallel execution safe because T5 doesn't need T4's actual file content — it only declares the dependency by name.
- **considered_options:**
  - Execute batch — chosen
  - Skip task — why_rejected: both tasks required for downstream commands
  - Sequential — why_rejected: T5 doesn't read T4's output; parallelism is safe
  - Pause — why_rejected: user proceeded

---

## execute.phase-4.batch-review.batch-2

- **severity:** notable
- **decision:** Fix T5 --sober self-contradiction surgically (agents/aron.md:179); accept T4 4 minors and T5 2 remaining minors with caveats; revisit only if T8 smoke verify exposes them
- **rationale:** T4 spec PASS code PASS (4 minor: Phase 4 YAML 'fences' wording conf 60, Phase 7 sub-section syntax under-specified conf 65, Phase 8 verdict gap on sub-argument rebuts conf 65, Phase 2→3 AST handoff implicit conf 55); T5 spec PASS code PASS (3 minor: --sober ack uses accent contradicting line 175 conf 80, --sober ack appears BEFORE Verdict callout violating CC-1 conf 70, 'Unsupported leap → Tempo loss' mapping stretchy conf 55). All structural_scope OK. T5 --sober contradiction is the only minor that surfaces immediately on first flag use — surgical fix justified. T4 polishes are below priority bar; T8 smoke verify will exercise the flagged code paths.
- **considered_options:**
  - Fix T5 --sober contradiction; accept rest with caveats — chosen
  - Fix all seven minor issues — why_rejected: each issue below priority bar; context cost not justified
  - Accept all with caveats — why_rejected: T5 --sober self-contradiction surfaces immediately on first flag use
  - Klaus review — why_rejected: no architectural ambiguity; minor polish issues

---

## execute.phase-5.merge-decision.batch-2

- **severity:** architectural
- **decision:** Merge each branch separately with --no-ff (preserve history) — two merge commits, T4 then T5
- **rationale:** "(no annotation; methodology default after batch 1 set precedent)". Conflict check: None detected (T4 owns skills/argument-audit/SKILL.md, T5 owns agents/aron.md; non-overlapping). Two merges executed: worktree-agent-ad2ca396d8532a159 (T4, +259 lines SKILL.md), worktree-agent-a4677a7b4fdf84fec (T5, +241 lines agents/aron.md including the --sober fix-up commit 0d6a8ca on top of 8d0d074).
- **considered_options:**
  - Merge each branch separately (preserve history) — chosen (consistent with batch-1 precedent)
  - Squash-merge each task — why_rejected: would lose T5 fix-up commit history
  - Single octopus merge — why_rejected: unconventional
  - Keep separate, don't merge — why_rejected: Batch 3 (commands) needs both files visible on main

---

## execute.phase-1.batch-start.batch-3

- **severity:** trivial
- **decision:** Execute batch 3 (parallel: T6 aron command + T9 aron-spar command)
- **rationale:** "(no annotation; selected from option list)". Both commands depend on the now-merged T5 (Aron agent name confirmed on main). Both must declare skills: argument-audit exactly — R-4 grep check enforced in acceptance criteria.
- **considered_options:**
  - Execute batch — chosen
  - Skip task — why_rejected: both commands required (T6 primary invocation, T9 sparring continuation; SC-10e and SC-8b both need them)
  - Sequential — why_rejected: T9 references T6 conceptually (--interactive flag points at it) but doesn't need T6's exact prose
  - Pause — why_rejected: user proceeded

---

## execute.phase-4.batch-review.batch-3

- **severity:** notable
- **decision:** Fix T9's 2 important issues surgically (Write-vs-Edit safety + turn-counting regex); accept T6 4 minors and T9 3 remaining minors with caveats
- **rationale:** T6 spec PASS code PASS (4 minor: slug TOCTOU race conf 70, slug Unicode handling conf 70, corpus 'path-like' fuzziness conf 55, <5-word edge case conf 35); T9 spec PASS code CONCERNS (2 important: append-vs-overwrite safety conf 80 — Write tool in allowed-tools could silently destroy prior audit, turn-counting under-specified conf 75 — malformed heading silently miscounts; 3 minor: parallel-invocation race conf 60, /end-spar leading slash misleading conf 55, Bash over-permissioned conf 50). All structural_scope OK. T9 important issues are the load-bearing concern — Write-vs-Edit is a data-loss vector, turn-counting is integrity-of-audit-file-as-source-of-truth. Both surgical to fix.
- **considered_options:**
  - Fix T9's 2 important; accept rest with caveats — chosen
  - Fix all important + minor — why_rejected: each minor below priority bar; context cost not justified for polish
  - Accept all with caveats — why_rejected: T9 Write-vs-Edit risk is data-loss vector that surfaces silently
  - Klaus review — why_rejected: issues are concrete and fix is obvious; no architectural ambiguity

---

## execute.phase-5.merge-decision.batch-3

- **severity:** architectural
- **decision:** Merge each branch separately with --no-ff (preserve history) — two merge commits, T6 then T9
- **rationale:** "(no annotation; methodology default after batch 1/2 precedent)". Conflict check: None detected (T6 owns commands/aron.md, T9 owns commands/aron-spar.md; non-overlapping). Two merges: worktree-agent-af19affe8e9021c20 (T6, +148 lines), worktree-agent-a77ec2918dbf093bb (T9, +157 lines including fix-up commit 0a9458e on top of b1f9488).
- **considered_options:**
  - Merge each branch separately (preserve history) — chosen (consistent with batch 1/2 precedent)
  - Squash-merge — why_rejected: would lose T9 fix-up commit history
  - Single octopus merge — why_rejected: unconventional
  - Keep separate — why_rejected: Batch 4 README needs both commands visible

---

## execute.phase-1.batch-start.batch-4

- **severity:** trivial
- **decision:** Execute batch 4 with T7 (babyclaude README update) + T8 hybrid (babyclaude structural verify + orchestrator smoke spawns of Aron against 1-2 fixtures)
- **rationale:** "(no annotation; selected from option list)". babyclaude lacks Task tool so cannot spawn Aron; structural verification (file existence, frontmatter, R-4 grep, plugin auto-discovery) suits babyclaude. Behavioural smoke (running Aron against fixtures) requires orchestrator-driven spawns. Full SC-4 through SC-10 behavioural validation explicitly deferred to /claudikins-kernel:verify next stage per the kernel methodology's strict-enforcement skill — execute owns "did it build" while verify owns "does it work".
- **considered_options:**
  - Execute batch (T7 + T8 hybrid) — chosen
  - T7 only; defer all T8 — why_rejected: no smoke at end of execute means problems surface only at verify stage; cheap early sanity check is worth it
  - T8 with full fixtures — why_rejected: 5+ Aron invocations expensive in tokens; /claudikins-kernel:verify is the proper home for full behavioural verification
  - Pause — why_rejected: user proceeded

---

## execute.phase-4.batch-review.batch-4

- **severity:** notable
- **decision:** Fix top-priority methodology gaps (1, 5, 6) from T8b smoke surface in skills/argument-audit/SKILL.md before merging Batch 4; T7 README accepted; T8 needs no merge (verification task, no files)
- **rationale:** Klaus-acting-as-Aron smoke produced 5/5 expected findings on hidden-assumption fixture (target ≥2), executed all 8 phases, classified 4 Walton schemes, fired 16 CQs, generated 4 Pollock-classified rebuttals (1 rebut + 3 undercuts). Persona maintained throughout. Surfaced 7 methodology gaps; user opted to fix priority 1 (Verdict-callout-first-line vs tool-calls/preamble carve-out), 5 (partial-rebut verdict rule for sub-argument vs top claim — overlaps with T4 code-review gap), 6 (argdown MCP probe mechanism). Gaps 2, 3, 4, 7 deferred to /claudikins-kernel:verify or v1.1.
- **considered_options:**
  - Accept and merge — why_rejected: priority gaps would propagate into verify stage and surface again as bugs
  - Fix top-priority gaps (1, 5, 6) — chosen
  - Accept + extra smoke against equivocation — why_rejected: would not address the gaps, just add more data showing the same gaps
  - Klaus review — why_rejected: gaps are concrete; fixes are surgical; no architectural ambiguity

---

## execute.phase-5.merge-decision.batch-4

- **severity:** architectural
- **decision:** Merge T7 README branch with --no-ff (consistent with batches 1-3 precedent). T8a structural verify produced no files to merge. T8b smoke (Klaus-as-Aron) produced no files to merge. SKILL.md v1.0.1 fix-up (gaps 1, 5, 6) committed directly on main as 702bc24 because babyclaude was blocked by permission prompts on the merged file.
- **rationale:** "(no annotation; methodology default after batches 1-3 precedent)". T7 owns README.md, no conflicts. The SKILL.md fix-up commit landed directly on main rather than via worktree because (a) the file was already merged on main, (b) babyclaude attempts to Edit/Write got permission-denied, (c) the orchestrator-direct fix-up was a tightly-scoped 3-edit surgical pass equivalent to what babyclaude was instructed to do.
- **considered_options:**
  - Merge T7; commit SKILL fix-up directly on main — chosen
  - Block on babyclaude permissions and skip the SKILL fix-ups — why_rejected: the three gaps are real and worth fixing per the batch 4 review decision
  - Squash-merge — why_rejected: T7 has only one substantive commit so equivalent; --no-ff preserves boundary for review-by-blame consistency with prior batches

---

## execute.handoff

- **severity:** trivial
- **decision:** Load /claudikins-kernel:verify
- **rationale:** "(no annotation; selected from option list)". Execute completed 9/9 tasks across 4 batches with 4 fix-up commits. T8b smoke produced 5/5 expected findings on hidden-assumption fixture; surfaced 7 methodology gaps (3 fixed in v1.0.1, 4 deferred to verify stage). Aron quartet ready for full behavioural verification with argdown plugin bound.
- **considered_options:**
  - Load /claudikins-kernel:verify — chosen
  - Stay here — why_rejected: user proceeded
  - Done for now — why_rejected: user wanted to continue to verify

---

## verify.phase-1.test-failure

<!-- decision: none, checkpoint: verify.phase-1.test-failure, reason: no test runner in markdown-only plugin project; adapted Phase 1 checks (plugin.json validity, frontmatter shape, R-4 grep, Klaus-trio independence, corpus literal-text, skill version patches) all passed on first run -->

---

## verify.phase-1.lint-autofix

<!-- decision: none, checkpoint: verify.phase-1.lint-autofix, reason: no lint runner in markdown-only plugin project -->

---

## verify.phase-1.type-check

<!-- decision: none, checkpoint: verify.phase-1.type-check, reason: no type system in markdown-only plugin project; YAML frontmatter structural-check folded into Phase 1 test_suite stage -->

---

## verify.phase-1.checkpoint

- **severity:** trivial
- **decision:** Continue to Output Verification with 8/8 Phase 1 adapted checks PASS
- **rationale:** Plugin.json valid JSON; all 4 new files have well-formed frontmatter with required keys (name/description/version/skills/argument-hint/allowed-tools as applicable); R-4 grep clean (3 argument-audit matches across new files, 0 drift); Klaus trio byte-identical to pre-Aron baseline confirmed via git hash-object comparison; corpus literal-text byte-match present in both corpus-contradiction files; SKILL v1.0.1 patches (probe procedure L49, verdict-callout carve-out L215, partial-rebut rule L244) all present.
- **considered_options:**
  - Continue to Output Verification — chosen
  - Re-run checks — why_rejected: all 8 passed; no failures to re-check
  - Abort — why_rejected: user proceeded

---

## verify.phase-2.catastrophiser

- **severity:** notable
- **decision:** Accept T8b smoke (Klaus-as-Aron on hidden-assumption fixture, captured during /claudikins-kernel:execute Batch 4) as catastrophiser evidence
- **rationale:** T8b is exactly the catastrophiser pattern ("see code working"): Klaus-acting-as-Aron audited tests/fixtures/audit/hidden-assumption.md end-to-end and hit 5 of 5 expected findings (target was ≥2). All 8 phases executed; 3 arguments classified across 4 Walton scheme assignments; 16 critical questions fired; 4 Pollock-classified rebuttals generated (1 rebut + 3 undercuts); Aron persona compliance maintained throughout. Surfaced 7 methodology gaps (3 fixed in skill v1.0.1 commit 702bc24, 4 deferred to /claudikins-kernel:verify or v1.1). Full evidence at .claude/reviews/code/8.json. Re-running another smoke would only re-validate what T8b already demonstrated; the 4 deferred gaps are scoping/edge-case refinements that don't block ship.
- **considered_options:**
  - Accept T8b as Phase 2 evidence — chosen
  - Run smoke on equivocation fixture — why_rejected: would validate v1.0.1 patches against a different argument type, but T8b already validated end-to-end execution; marginal value
  - Run corpus-mode smoke — why_rejected: would validate SC-9 specifically; deferred to actual production use rather than synthetic smoke
  - Run all three smokes — why_rejected: maximum coverage but high context cost; no critical concerns from T8b warrant it

---

## verify.phase-3.cynic-optin

- **severity:** trivial
- **decision:** Run cynic on the skill body (skills/argument-audit/SKILL.md)
- **rationale:** "(no annotation; selected from option list)". Skill body is 264 lines (v1.0.1); cynic may tighten redundant phrasing or over-explanation. Agent body and references deferred — agent persona theatre is load-bearing; references are domain catalogues that benefit from explicit redundancy.
- **considered_options:**
  - Skip cynic — why_rejected: skill body has accumulated 4 surgical patches; a coherence pass could improve readability
  - Run cynic on skill body — chosen
  - Run cynic on agent body — why_rejected: persona content is load-bearing; cynic might strip engagement

---

## verify.phase-5.gate

- **severity:** architectural
- **decision:** Ready to Ship — unlock_ship flipped to true; verified_commit_sha recorded; manifest generated
- **rationale:** Verification Report (verbatim summary): Phase 1 8/8 adapted plugin checks PASS (plugin.json validity, frontmatter shape across all 4 new files, R-4 grep clean 3-of-3 with 0 drift, Klaus-trio byte-identical to pre-Aron baseline via git hash-object, corpus literal-text byte-match present in both corpus-contradiction files, SKILL v1.0.1 patches present at L4/L49/L215/L244); Phase 2 catastrophiser-equivalent T8b PASS (Klaus-as-Aron audit of hidden-assumption.md hit 5 of 5 expected findings vs target ≥2, all 8 phases executed end-to-end, 16 CQs fired across 3 arguments and 4 Walton scheme assignments, 4 Pollock-classified rebuttals 1 rebut + 3 undercuts, Aron persona compliance maintained throughout, methodology survived degraded mode); Phase 3 cynic polish on skill body running in background as optional non-gating finalisation. Deferred items: 4 of 7 T8b methodology gaps to v1.1, T6 4 minor polish nits, T9 3 minor polish nits, worktree cleanup. All deferred items are documented in .claude/reviews/code/{4,6,9}.json and the Tier-1 log; none are shipping blockers. The Aron quartet (agent + skill + 2 commands + 4 references + 10 fixtures + README addition) ships independently of Klaus trio which remains byte-identical to baseline.
- **considered_options:**
  - Ready to Ship — chosen
  - Accept with Caveats — why_rejected: deferred items are already documented in reviews JSON + Tier-1 log; explicit caveat list would duplicate without adding shipping risk
  - Needs Work — why_rejected: all phase checks PASS; smoke surface gaps are v1.1 refinements not blockers

---

## verify.handoff

- **severity:** trivial
- **decision:** Load /claudikins-kernel:ship
- **rationale:** "(no annotation; selected from option list)"
- **considered_options:**
  - Load /claudikins-kernel:ship — chosen
  - Stay here — why_rejected: user proceeded
  - Done for now — why_rejected: user proceeded

---

## ship.stage-1.preship-review

- **severity:** notable
- **decision:** Continue to Commit Strategy — pre-ship review accepted
- **rationale:** "(no annotation; selected from option list)". Pre-ship summary: verify session verify-2026-05-15-1145 unlock_ship=true; verified commit 9feb661 matches current HEAD (code integrity PASS); remote origin git@github.com:kellenff/claudikins-klaus.git configured; 10 first-parent commits to ship comprising 9 task merges + 1 cynic polish + 4 underlying fix-up commits; aggregate diff 19 files +3,374 lines additive only; Klaus trio byte-identical to baseline; T8b smoke 5/5 expected findings.
- **considered_options:**
  - Continue to Commit Strategy — chosen
  - Review Evidence — why_rejected: reviews already inspected during execute/verify; would duplicate
  - Back to Verify — why_rejected: verify gate satisfied, HEAD = verified commit
  - Abort — why_rejected: user proceeded

---

## ship.stage-2.commit-strategy

- **severity:** architectural
- **decision:** Direct push to remote main (no PR); preserve full 24-commit history including task merges, fix-ups, cynic polish, and skill v1.0.1
- **rationale:** "(no annotation; selected from option list)". Solo-owned plugin (single maintainer); no parallel contributors needing the PR review gate. Full per-task history is the Tier-1 audit trail's commit-level companion and is best preserved. The PR-gate methodology rule is explicitly waived for solo plugin maintenance; this decision is recorded as the architectural precedent for future Aron-like additions to this repo.
- **considered_options:**
  - Push current main as feature branch + open PR — why_rejected: solo-owned plugin with no co-maintainers; PR review gate is ceremonial overhead without functional benefit
  - Squash-merge to single PR commit — why_rejected: would lose task-level merge structure and fix-up trail; Tier-1 log audit value diminished
  - Direct push to remote main (no PR) — chosen
  - Tag-only release — why_rejected: this is the first Aron release; main should track it for clarity. Tag can come separately as a versioned snapshot.

---

## ship.stage-2.commit-message

<!-- decision: none, checkpoint: ship.stage-2.commit-message, reason: direct-push strategy preserves existing per-commit messages; no squash commit message drafted -->

---

## ship.stage-2-5.adr-synthesis

- **severity:** architectural
- **decision:** PENDING (atomic-commit in progress)
- **rationale:** Synthesis ready_for_user_approval; user selected "Accept synthesis + atomic commit". 8 items routed across 5 sections (PURPOSE 1, STACK 1, PATTERNS 2, TRADEOFFS 3, PHILOSOPHY 1; ARCHITECTURE 0 placeholder). ADR size 7262/8000 bytes (90.8%); no rollup needed; no spillover. Routing audit at .claude/agent-outputs/docs/adr-routing-audit-plan-2026-05-15-1137.json. Synthesiser flagged two routings as worth-reviewing (ADR-001 → PHILOSOPHY, ADR-007 → STACK); user accepted as-is.
- **considered_options:**
  - Accept synthesis + atomic commit — chosen
  - Review individual items — why_rejected: synthesiser's keyword-driven defaults are sensible; ARCHITECTURE empty acceptable for first-ADR
  - Skip ADR (this run only) — why_rejected: first-ADR session for project; valuable to land canonical record now
  - Abort ship — why_rejected: user proceeded
- **outcome:** pending (atomic_commit_sha to be filled after commit lands)
